# Python dependencies for Decision Environment
# These are needed by event source plugins and collections

# AWS SDK — required by community.aws and ansible.eda.aws_sqs_queue
boto3>=1.34.0
botocore>=1.34.0

# HTTP requests — used by webhook filters and custom sources
requests>=2.31.0

# JSON Schema validation (used by ansible.eda filters)
jsonschema>=4.0.0

# asyncio-based aiohttp — required by ansible.eda webhook source
aiohttp>=3.9.0

# Kafka client — needed if using ansible.eda.kafka source
# aiokafka>=0.10.0  # Uncomment if using Kafka

# Azure SDK — needed if using Azure event grid source
# azure-servicebus>=7.0.0  # Uncomment if using Azure

# YAML parsing
PyYAML>=6.0
